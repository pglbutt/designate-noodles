method: POST
uri: /v2/zones
headers:
    Content-type: application/json
    Accept: application/json
body: |
    {
        "name": "{{name}}",
        "email": "{{email : spag@pglbutt.com }}",
        "ttl": 3600
    }
